# nixpacks.toml
[phases.setup]
nixPkgs = ['...', 'stripe-cli','redis']
aptPkgs = ["...", "update" ,"libappindicator1"]
cmds = ['...','wget -c https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb', 'sudo dpkg -i google-chrome-stable_current_amd64.deb']